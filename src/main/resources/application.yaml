server:
  app:
    auto: true
  servlet:
    context-path: /apiTest/v1

spring:
  application:
    name: apiTest

#Spring boot actuator config for health check
endpoints:
  health:
    sensitive: false
    mapping:
      DOWN: SERVICE_UNAVAILABLE

